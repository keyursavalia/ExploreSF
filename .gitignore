# Claude Code session documentation — local only, never push
/.claude/

# Design assets and local resources — never commit (root-anchored to avoid matching ExploreSF/Resources/)
/resources/

# API keys and secrets — never commit; copy Config.template.txt → Config.swift
ExploreSF/Config.swift
ExploreSF/Config 2.swift

# macOS
.DS_Store
**/.DS_Store

# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
*.orig
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but just in case)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# Other
*.log
